export default function FormLabel({ labelText }) {
    return (
        <label className="fieldset-label text-gray-900 text-lg font-semibold min-w-[400px]">
            {labelText}
        </label>
    );
}
